{
  "name": "buck-urbit",
  "version": "1.0.0",
  "scripts": {
    "test": "forge test -vvv",
    "build": "forge build",
    "deploy:sepolia": "forge script script/Deploy.s.sol --rpc-url $SEPOLIA_URL --broadcast --verify",
    "deploy:mainnet": "forge script script/Deploy.s.sol --rpc-url $MAINNET_URL --broadcast --verify",
    "deploy:local": "forge script script/DeployLocal.s.sol --rpc-url http://localhost:8545 --broadcast",
    "node": "anvil"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0"
  }
}
